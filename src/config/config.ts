export const config = {
    env_dev: {
        // host: "http://127.0.0.1:4098", // ======= > DEV
        host: '' + '', // ============================ > PROD
        ws_host: '127.0.0.1' // HOST YOU DEPLOYED THIS BUILD INTO
    }
}